# ── AI Providers ─────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...          # Optional — used as fallback if Claude is unavailable

# ── G-Force Auth ──────────────────────────────────────────────────────────────
# Set a strong random secret. All /api/ routes require this in the x-gforce-key header.
# Generate one: openssl rand -hex 32
GFORCE_API_KEY=

# Exposed to the browser so the UI can authenticate against the API.
# Set to the same value as GFORCE_API_KEY.
NEXT_PUBLIC_GFORCE_API_KEY=
